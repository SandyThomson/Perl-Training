#!/usr/bin/perl

use strict;
use warnings;

use Getopt::Long;
use File::Slurp;

# Find the indexes of occurences of a string within a larger string

# Where is the large string coming from? File? stdin?
# Same with the small string

